{
  "name": "mock-dash-workspace",
  "version": "0.0.0",
  "description": "Root package for the library monorepo.",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "biome check --write",
    "check": "pnpm -r check",
    "dev:react": "pnpm --filter react-example dev",
    "dev:vue": "pnpm --filter vue-example dev"
  },
  "dependencies": {
    "hono": "^4.9.12"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.0.0",
    "@types/node": "^24.7.2",
    "@vitest/coverage-v8": "^3.2.4",
    "prettier": "^3.6.2",
    "rollup-plugin-visualizer": "^6.0.4",
    "typescript": "^5.9.3",
    "vite": "^7.1.9",
    "zocker": "^3.0.0",
    "zod": "^4.1.12"
  }
}
